plugins {
    id "java"
}

version = "1.0.0"
group = "fr.tangv.mtcbs"

sourceCompatibility = 8
targetCompatibility = 8

compileJava.options.encoding = 'UTF-8'
javadoc.options.encoding = 'UTF-8'

repositories {
}

dependencies {
}

jar {
    baseName = "${rootProject.name}"
    version = "${project.version}"
}